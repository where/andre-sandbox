<?xml version="1.0" encoding="UTF-8"?>
<beans 
  xmlns="http://www.springframework.org/schema/beans"
  xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
  xmlns:util="http://www.springframework.org/schema/util"
  xsi:schemaLocation="http://www.springframework.org/schema/beans
    http://www.springframework.org/schema/beans/spring-beans-3.0.xsd
    http://www.springframework.org/schema/util http://www.springframework.org/schema/util/spring-util-3.0.xsd"
  >

  <bean id="propertyConfigurer" class="org.springframework.beans.factory.config.PropertyPlaceholderConfigurer">
    <property name="locations" >
      <list>
        <value>classpath:appContext.properties</value>
      </list>
    </property>
    <property name="systemPropertiesModeName" value="SYSTEM_PROPERTIES_MODE_OVERRIDE" />
    <property name="ignoreResourceNotFound" value="true"/>
  </bean>

  <bean id="dataSource1" class="org.apache.commons.dbcp.BasicDataSource">
    <property name="driverClassName" value="${jdbc.driverClassName}"/>
    <property name="url" value="${jdbc.url.1}"/>
    <property name="username" value="${jdbc.username.1}"/>
    <property name="password" value="${jdbc.password.1}"/>
  </bean>

  <bean id="dataSource2" class="org.apache.commons.dbcp.BasicDataSource">
    <property name="driverClassName" value="${jdbc.driverClassName}"/>
    <property name="url" value="${jdbc.url.2}"/>
    <property name="username" value="${jdbc.username.2}"/>
    <property name="password" value="${jdbc.password.2}"/>
  </bean>

  <util:list id="queries" >
     <value>select count(*) from tba_sandbox.dump_hourly_pubs_nets_platforms_total_copy</value>
     <value>select * from tba_sandbox.dump_hourly_pubs_nets_platforms_total_copy limit 10</value>
     <value>select * from tba_sandbox.dump_hourly_nets_zip_total limit 10</value>
  </util:list>

</beans>

